@model IEnumerable<AplicatieCamine.Models.Administratori>

@{
    ViewData["Title"] = "Index";
}
<link rel="stylesheet" href="~/css/styleTable.css">
<style>
    main {
        flex-direction: column;
    }

    .create_button {
        border: none;
        color: white;
        background-color: #008CBA;
        padding: 5px 15px;
        text-align: center;
        text-decoration: none;
        font-size: 16px;
        cursor: pointer;
        transition-duration: 0.6s;
        margin: 5px 0 5px 10px;
    }
</style>
<div class="container-fluid d-flex justify-content-center mb-auto">
    <div class="card">
        <div class="card-header header-elements-inline">
            <h5 style="font-family:Garamond,serif;font-size: 40px">Administratori</h5>
            <div class="mb-2">
                <a asp-action="Create" class="create_button">Adaugă!</a>
                <a asp-controller="Home" asp-action="Index" class="create_button">Înapoi</a>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>
                            ID Admin
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Nume)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Adresa)
                        </th>
                        <th>
                            Tel.
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Email)
                        </th>
                        <th>
                            ID Cămin
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.IdAdmin)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nume)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Adresa)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NrTelefon)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IdCaminNavigation.IdCamin)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.IdAdmin">Editeză</a> |
                                <a asp-action="Details" asp-route-id="@item.IdAdmin">Detalii</a> |
                                <a asp-action="Delete" asp-route-id="@item.IdAdmin">Șterge</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>